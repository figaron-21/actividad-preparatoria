## Configuración del Entorno

Para ejecutar este proyecto, necesitas crear un archivo `.env` en la raíz del proyecto y configurar las siguientes variables de entorno:

```env
DATABASE_URL="tu_ruta_de_base_de_datos"
SECRET_KEY="una_clave_secreta_muy_segura"
DEBUG=True
PORT=8000